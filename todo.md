# AmengExplorer Todo

> 生成日期：2026-08-16
> 按可实现性排序，P0 = 用户可直接感知的缺陷/高价值，P1 = 体验增强，P2 = 锦上添花

## P0 修复（用户点名）

- [x] **右键菜单项目错位/定位修复**：修复 `.context-menu-item span:first-of-type` 误匹配图标占位导致的对齐错乱（改为 `:not(.icon-wrapper):not(.context-menu-shortcut)`）；移除 shell 菜单项的空图标占位；子菜单加限高滚动。
- [x] **右键 Shell 菜单“有时加载失败”修复**：脚本支持驱动器根目录（Namespace 回退 + `Self`）；过滤 shell 扩展日志行污染；无 pwsh 时友好提示；失败提供“重试”；temp 脚本 `finally` 兜底清理；超时放宽到 15s。
- [ ] **亮色模式观感优化**：`style.css` 仍有硬编码深色残留（`.loading-screen` 固定 `hsl(225 10% 16%)`、`#0078d4` 按钮、`--shadow-*` 深色阴影、弹层半透明在浅色下偏灰），浅色变量覆盖不完整。
  - 方案：全量扫描硬编码颜色改为变量；补齐 `[data-theme="light"]` 下的 loading 屏/阴影/滚动条/选择高亮；深色与浅色各截图对比验收。
- [x] **增加更多主题色 + 自定义取色**：预置色块扩到 10 个（新增 teal/pink/yellow/cyan/slate），并新增 `<input type="color">` 自定义取色（hex→HSL 应用到 `--primary`/`--accent`，持久化存储 hex）。

## P1 功能增强

- [ ] **驱动器卡片容量进度条**：`getDriveUsage()` 已实现（pwsh `Get-Volume` 容量），但 `renderDrives` 只渲染 root 卡片，本地磁盘卡片未接入 usage；把容量/已用/进度条接到 `/media/<盘符>` 卡片。
- [ ] **Shift+Click 范围选择**：多选目前仅 Ctrl+Click / Ctrl+A；实现 Shift 连续范围选择（列表与网格视图）。
- [ ] **标签页会话恢复**：重启后恢复所有标签页及各自路径（当前仅有单路径 `lastDirectory`），便于多任务场景。
- [ ] **地址栏自动补全与历史**：输入时按虚拟路径前缀补全（amsys `resolve` 校验存在性），并记录最近访问路径。
- [ ] **内置文件预览面板**：信息面板扩展图片/文本/Markdown 内联预览（当前仅占位图标），PDF 可后续加。
- [ ] **回收站恢复原路径**：删除时记录来源路径，恢复时回到原位置（当前恢复到回收站父目录）。
- [ ] **书签/收藏夹**：侧栏快速访问区支持自定义收藏目录。

## P1 性能

- [ ] **大目录虚拟滚动**：当前一次性渲染全部条目，数万文件目录会卡；列表/网格改按需渲染（IntersectionObserver 或虚拟窗口）。
- [ ] **文件索引增量更新**：`search-index-worker` 每次全盘重建；改为记录目录 mtime 增量扫描或分盘并行。
- [ ] **图标加载缓存**：`icons.js` 每次 `loadIcon` 都读磁盘 SVG；启动时一次性加载所需图标并缓存字符串。
- [ ] **amsys 请求并发/预取**：`amsys-client` 串行队列，目录切换可预取相邻路径（resolve + list_dir）。

## P2 体验与发布

- [ ] **主窗口背景质感**：Mica/Acrylic（WinUI 风格）或轻量半透明，深/浅色下分别适配。
- [ ] **检查更新后一键下载更新**：当前“前往下载”跳 GitHub；后续可下载 zip 资产并引导解压替换（便携版）。
- [ ] **首次运行向导**：设置横幅/主题/默认目录/用户，避免冷启动空白。
- [ ] **README/发布说明整理**：功能清单、截图、常见问题（Everything 已移除、pwsh7 依赖说明）。

## 已排除（明确不做的方向）

- 字体大小设置（已移除，用户决定）
- 图标库裁剪（体积可接受）
- 空壳目录清理（无影响）
- Everything 搜索（已彻底移除）
