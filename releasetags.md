# Release Tags

本文件记录 AmengExplorer 各版本的 release tag 及完整功能说明，内容可直接用于 GitHub Releases 发布说明。

---

## v1.2.0 — 首个正式版

- **Tag**: `v1.2.0`
- **Commit**: `d0e7f13`
- **版本**: `package.json` version = 1.2.0（`app.getVersion()` 与 tag 一致，检查更新可正确比对）
- **发布日期**: 2026-08-09

### 功能总览（全量）

AmengExplorer 是一个基于 Electron 的 Windows 文件管理器，提供 Linux 风格的文件管理体验。

#### 文件浏览

- 标签页浏览：多标签切换、新建/关闭标签页
- 双窗格布局：左右窗格独立导航、独立列表/网格视图、窗格同步目录
- 三种视图模式：列表、网格、分栏（列）视图
- 导航：前进 / 后退 / 向上 / 主页 / 刷新，窗格级独立导航控件
- 侧边栏快捷入口：主页、桌面、下载、文档、图片、音乐、视频、回收站、磁盘（C: / D:）、网络
- 地址栏直接输入路径跳转
- 目录大小统计：手动计算（带进度任务）、自动统计子文件夹大小、符号链接目录同样支持
- 大小缓存：已计算目录缓存结果，可一键重新计算

#### 文件操作

- 打开 / 在新标签页中打开
- 复制、剪切、粘贴（带任务进度，支持复制/移动队列）
- 重命名（F2）、删除（Delete）
- 新建文件（Ctrl+N）、新建文件夹（Ctrl+Shift+N）
- 属性查看（Alt+Enter）
- 跨卷移动兜底：EXDEV 时自动复制 + 删除
- 多选：Ctrl+Click 多选/取消、Ctrl+A 全选

#### 内置回收站

- 程序内置虚拟根回收站：root 用户为 `<root>\root\Trash`，其他用户为 `<root>\home\<用户>\Trash`（当前用户来自 `root/etc/users.toml`）
- 首次使用自动创建
- 删除进回收站 / 回收站内永久删除
- 回收站内右键支持"恢复"与"永久删除"
- 同名冲突自动添加 ` (1)`, ` (2)` 后缀

#### 启动台（Ctrl+P）

- 搜索模式：输入文件名搜索，Enter 打开结果
- 运行模式：输入命令执行，Enter 运行；支持 `>` 前缀自动切换
- 模式切换：点击输入框右侧"搜索/运行"标签
- Ctrl+Enter 定位文件到资源管理器
- 最近搜索（最多 20 条）/ 最近运行（最多 10 条）历史记录，支持手动清空
- 历史仅在主动提交时记录，防抖搜索不记录

#### 搜索与索引

- 本地文件索引搜索：Worker 线程后台构建索引，不阻塞主线程
- 搜索深度、自动索引可配置（设置面板）
- PATH 快速扫描兜底：索引未就绪时也能返回结果
- 手动重建索引

#### 任务进度

- 任务面板（工具栏图标带数量徽标）
- 复制、移动、目录大小统计等长任务实时进度条与状态
- 支持暂停 / 恢复 / 取消

#### 信息与筛选

- 信息面板：查看选中文件详情
- 筛选面板：按名称、文件类型（文件夹/图片/视频/音频/文档/代码/压缩包/可执行/字体等）过滤

#### 设置面板

- 通用：启动页面、默认视图、语言、删除确认、显示隐藏文件、双击打开
- 搜索：自动索引、搜索深度、历史记录
- 外观：深浅色主题、强调色、主页横幅图片（URL 或本地图片）
- 关于：应用信息、配置文件位置、检查更新、恢复默认设置

#### 系统集成

- amsys 集成：外部 amsys 动态解析（config.ini `amsys=` 配置，支持绝对路径或类 Unix 路径）、管道通信客户端
- PowerShell 路径解析：优先程序目录内置 `pwsh7/pwsh.exe`（`Get-CimInstance` 磁盘枚举/容量），无 pwsh 时降级为盘符扫描，兼容 Windows PE
- 检查更新：GitHub Releases API（`releases/latest`）semver 对比，有新版时提示"前往下载"

#### 界面与图标

- Fluent 图标体系：`data-icon` 属性 + SVG sprite
- 图标映射：`config/icons.json` `_types` 扩展名表直接匹配图标库
- 符号链接目录使用 Folder Link 图标、归档文件使用 Folder Zip
- 删除到回收站/剪切等操作有视觉反馈

#### 便携版支持

- 所有用户数据存储在程序基础路径下 `config/` 目录（settings.json、icons.json、launchpad-history.json）
- 渲染进程不直接读写配置文件，全部通过 IPC 主进程处理
- 同步 IPC 仅用于模块加载初始化（图标配置），常规读写走异步 IPC

### 技术栈

- Electron 41 + electron-forge
- Node.js CommonJS，原生 HTML/CSS/JS（无前端框架）
- Worker 线程构建搜索索引与大小统计

---

<!-- 后续版本在此追加 -->
